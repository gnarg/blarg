<dt>Recent</dt>
<dd>
  <ul class="dates">
    <% @recent_posts.each do |post| %>
    <li><a href="/posts/<%= post.document_id %>">
      <span class="date"><%= post.created_at.strftime('%b %d %Y') %></span>
      <%= post.title %>
    </a></li>
    <% end %>
  </ul>
</dd>
